<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath /> <!-- lookup parent from repository -->
	</parent>
	<groupId>edu.xtd</groupId>
	<artifactId>facturacion360</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>facturacion360</name>
	<description>Proeycto web para gestión de facturas online</description>
	<url />
	<licenses>
		<license />
	</licenses>
	<developers>
		<developer />
	</developers>
	<scm>
		<connection />
		<developerConnection />
		<tag />
		<url />
	</scm>
	<properties>
		<java.version>21</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-jdbc</artifactId>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Documentación OpenAPI y Swagger UI -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>3.0.3</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-jdbc-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!--
			Testcontainers 2.x cambio los identificadores de artefacto: los modulos llevan
			prefijo. Es testcontainers-mysql, NO mysql, que se quedo en 1.21.4 y no existe
			en la 2.0.5 que gestiona Spring Boot 4.1.0. Por eso van sin <version>: el
			spring-boot-dependencies importa el testcontainers-bom.
		-->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-mysql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!--
				Declara la etiqueta @autor, que permite atribuir METODOS concretos dentro de
				clases compartidas. El @author estandar solo vale en clases e interfaces.
				placement "tm" = tipos y metodos.

				NO BORRAR sin quitar antes las etiquetas @autor del codigo: sin esta
				configuracion, javadoc las trata como desconocidas y falla la generacion.
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<configuration>
					<tags>
						<tag>
							<name>autor</name>
							<placement>tm</placement>
							<head>Autor:</head>
						</tag>
					</tags>
				</configuration>
			</plugin>

			<!--
				Cobertura. SIN umbral que rompa la compilacion, a proposito: un umbral
				puesto el mismo dia que el plugin, sobre codigo que nunca lo ha tenido,
				o es tan bajo que no dice nada, o deja el proyecto en rojo por algo que
				no se acaba de romper. Primero se mide; el umbral se pone cuando haya
				una cifra real que defender.
				Informe en target/site/jacoco/index.html
			-->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.15</version>
				<executions>
					<execution>
						<id>preparar-agente</id>
						<goals><goal>prepare-agent</goal></goals>
					</execution>
					<execution>
						<id>informe</id>
						<phase>verify</phase>
						<goals><goal>report</goal></goals>
					</execution>
				</executions>
			</plugin>

			<!--
				Analisis estatico. failOnError a false por la misma razon que JaCoCo no
				lleva umbral: primero se ve que dice sobre el codigo que ya hay.
			-->
			<plugin>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-maven-plugin</artifactId>
				<version>4.10.4.1</version>
				<configuration>
					<failOnError>false</failOnError>
					<effort>Max</effort>
					<threshold>Default</threshold>
				</configuration>
			</plugin>

			<!--
				Formato. ratchetFrom es lo importante: sin el, la primera ejecucion
				reformatea los 58 ficheros del proyecto y destroza git blame en un commit
				de infraestructura. Con el, solo mira lo que la rama haya tocado.
				Y solo tres reglas, ninguna que mueva codigo: quitar imports sin usar,
				quitar espacios al final y dejar salto de linea final.
			-->
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>3.10.2</version>
				<configuration>
					<ratchetFrom>origin/master</ratchetFrom>
					<java>
						<removeUnusedImports />
						<trimTrailingWhitespace />
						<endWithNewline />
					</java>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
